#!/bin/sh

while read local_ref local_sha remote_ref remote_sha
do
  if [ "$remote_ref" = "refs/heads/master" ]; then
    echo "禁止直接推送到 master。请创建功能分支并通过 Pull Request 合并。"
    exit 1
  fi
done

exit 0

